import type { PokemonItem } from '../types/pokemon';
type CardProps = {
    pokemon: PokemonItem;
};
export declare const Card: ({ pokemon }: CardProps) => import("react/jsx-runtime").JSX.Element;
export {};
